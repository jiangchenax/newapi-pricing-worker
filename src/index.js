// MOSS New API Pricing Worker V2.2
// /pricing only · all models · no pagination
// hero removed · filter kept · details kept · larger typography

const GLOBAL_CSS = "\n/* MOSS Pricing V2.2\n   - no hero\n   - keep filter\n   - keep details\n   - larger typography\n   - all models on one page\n*/\n\n#moss-pricing-app {\n  --bg: #f7f9fc;\n  --panel: rgba(255,255,255,.94);\n  --panel-strong: #fff;\n  --line: rgba(17,24,39,.09);\n  --line-strong: rgba(17,24,39,.15);\n  --text: #111827;\n  --muted: #667085;\n  --muted-2: #98a2b3;\n  --accent: #6f7cff;\n  --accent-soft: rgba(111,124,255,.08);\n  --green: #079455;\n\n  position: fixed;\n  inset: 64px 0 0 0;\n  z-index: 30;\n\n  overflow-y: auto;\n  overflow-x: hidden;\n\n  background:\n    radial-gradient(1000px 520px at 50% -260px, rgba(93,112,255,.10), transparent 72%),\n    var(--bg);\n\n  color: var(--text);\n\n  font-family:\n    Inter,\n    ui-sans-serif,\n    system-ui,\n    -apple-system,\n    BlinkMacSystemFont,\n    \"Segoe UI\",\n    sans-serif;\n\n  -webkit-font-smoothing: antialiased;\n  text-rendering: optimizeLegibility;\n}\n\nhtml.dark #moss-pricing-app,\n.dark #moss-pricing-app {\n  --bg: #090c12;\n  --panel: rgba(17,21,28,.92);\n  --panel-strong: #11161e;\n  --line: rgba(255,255,255,.085);\n  --line-strong: rgba(255,255,255,.15);\n  --text: rgba(247,249,253,.96);\n  --muted: rgba(218,224,234,.62);\n  --muted-2: rgba(218,224,234,.38);\n  --accent: #91a7ff;\n  --accent-soft: rgba(145,167,255,.09);\n  --green: #75d5a4;\n\n  background:\n    radial-gradient(1000px 520px at 50% -260px, rgba(93,112,215,.13), transparent 72%),\n    var(--bg);\n}\n\n#moss-pricing-app * {\n  box-sizing: border-box;\n}\n\n#moss-pricing-app button,\n#moss-pricing-app input,\n#moss-pricing-app select {\n  font: inherit;\n}\n\n#moss-pricing-app .market-shell {\n  width: min(1500px, calc(100vw - 38px));\n  margin: 0 auto;\n  padding: 26px 0 110px;\n}\n\n/* ---------- top toolbar ---------- */\n\n#moss-pricing-app .market-topbar {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 18px;\n\n  margin-bottom: 16px;\n}\n\n#moss-pricing-app .market-title-wrap {\n  min-width: 0;\n}\n\n#moss-pricing-app .market-title {\n  margin: 0;\n  color: var(--text);\n  font-size: 24px;\n  line-height: 1.15;\n  font-weight: 760;\n  letter-spacing: -.03em;\n}\n\n#moss-pricing-app .market-subtitle {\n  margin: 6px 0 0;\n  color: var(--muted);\n  font-size: 13px;\n  line-height: 1.5;\n}\n\n#moss-pricing-app .market-count {\n  color: var(--text);\n  font-weight: 760;\n}\n\n#moss-pricing-app .search-wrap {\n  position: relative;\n  flex: 0 1 430px;\n  min-width: 280px;\n}\n\n#moss-pricing-app .search-icon {\n  position: absolute;\n  left: 13px;\n  top: 50%;\n  width: 17px;\n  height: 17px;\n  transform: translateY(-50%);\n  color: var(--muted-2);\n  pointer-events: none;\n}\n\n#moss-pricing-app .search-input {\n  width: 100%;\n  height: 46px;\n  padding: 0 14px 0 42px;\n\n  outline: none;\n\n  border: 1px solid var(--line);\n  border-radius: 11px;\n\n  background: var(--panel);\n  color: var(--text);\n\n  font-size: 14px;\n\n  box-shadow:\n    inset 0 1px 0 rgba(255,255,255,.03),\n    0 7px 24px rgba(0,0,0,.025);\n}\n\n#moss-pricing-app .search-input::placeholder {\n  color: var(--muted-2);\n}\n\n#moss-pricing-app .search-input:focus {\n  border-color: color-mix(in srgb, var(--accent) 34%, transparent);\n  box-shadow: 0 0 0 3px var(--accent-soft);\n}\n\n/* ---------- main layout ---------- */\n\n#moss-pricing-app .market-layout {\n  display: grid;\n  grid-template-columns: 285px minmax(0,1fr);\n  align-items: start;\n  gap: 16px;\n}\n\n/* ---------- filter ---------- */\n\n#moss-pricing-app .filter-panel {\n  position: sticky;\n  top: 16px;\n\n  overflow: hidden;\n\n  border: 1px solid var(--line);\n  border-radius: 16px;\n\n  background: var(--panel);\n\n  box-shadow:\n    inset 0 1px 0 rgba(255,255,255,.03),\n    0 8px 28px rgba(0,0,0,.025);\n}\n\n#moss-pricing-app .filter-head {\n  display: flex;\n  align-items: flex-start;\n  justify-content: space-between;\n  gap: 12px;\n\n  padding: 18px 18px 14px;\n}\n\n#moss-pricing-app .filter-head h2 {\n  margin: 0;\n  color: var(--text);\n  font-size: 19px;\n  line-height: 1.2;\n  font-weight: 760;\n}\n\n#moss-pricing-app .filter-head p {\n  margin: 7px 0 0;\n  color: var(--muted);\n  font-size: 13px;\n  line-height: 1.55;\n}\n\n#moss-pricing-app .reset-btn {\n  flex: 0 0 auto;\n\n  min-height: 32px;\n  padding: 0 8px;\n\n  border: 0;\n  border-radius: 8px;\n\n  background: transparent;\n  color: var(--muted);\n\n  font-size: 13px;\n  font-weight: 600;\n\n  cursor: pointer;\n}\n\n#moss-pricing-app .reset-btn:hover {\n  background: var(--accent-soft);\n  color: var(--text);\n}\n\n#moss-pricing-app .filter-section {\n  margin: 0 18px;\n  padding: 15px 0 17px;\n\n  border-top: 1px solid var(--line);\n}\n\n#moss-pricing-app .filter-section:first-of-type {\n  border-top: 0;\n}\n\n#moss-pricing-app .filter-section-title {\n  margin: 0 0 11px;\n\n  color: var(--text);\n\n  font-size: 16px;\n  font-weight: 730;\n}\n\n#moss-pricing-app .filter-chips {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 8px;\n}\n\n#moss-pricing-app .filter-chip {\n  min-height: 34px;\n  padding: 0 11px;\n\n  display: inline-flex;\n  align-items: center;\n  gap: 7px;\n\n  border: 1px solid var(--line);\n  border-radius: 999px;\n\n  background: var(--panel-strong);\n  color: var(--muted);\n\n  font-size: 13px;\n  font-weight: 590;\n\n  cursor: pointer;\n\n  transition:\n    background .14s ease,\n    border-color .14s ease,\n    color .14s ease,\n    transform .12s ease;\n}\n\n#moss-pricing-app .filter-chip:hover {\n  border-color: var(--line-strong);\n  color: var(--text);\n}\n\n#moss-pricing-app .filter-chip[aria-pressed=\"true\"] {\n  border-color: color-mix(in srgb, var(--accent) 35%, var(--line));\n  background: var(--accent-soft);\n  color: var(--text);\n  box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--accent) 8%, transparent);\n}\n\n#moss-pricing-app .filter-chip-count {\n  min-width: 23px;\n  height: 23px;\n  padding: 0 6px;\n\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n\n  border-radius: 999px;\n\n  background: color-mix(in srgb, var(--muted) 9%, transparent);\n  color: var(--muted);\n\n  font-size: 12px;\n  font-weight: 650;\n}\n\n/* ---------- result area ---------- */\n\n#moss-pricing-app .result-panel {\n  min-width: 0;\n}\n\n#moss-pricing-app .result-head {\n  min-height: 50px;\n\n  padding: 0 4px 10px;\n\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 16px;\n}\n\n#moss-pricing-app .result-info {\n  color: var(--muted);\n  font-size: 13px;\n}\n\n#moss-pricing-app .result-info strong {\n  color: var(--text);\n  font-size: 15px;\n  font-weight: 760;\n}\n\n#moss-pricing-app .sort-select {\n  height: 36px;\n  padding: 0 30px 0 10px;\n\n  border: 1px solid var(--line);\n  border-radius: 9px;\n\n  outline: none;\n\n  background: var(--panel);\n  color: var(--text);\n\n  font-size: 13px;\n}\n\n/* ---------- card grid ---------- */\n\n#moss-pricing-app .model-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0,1fr));\n  gap: 10px;\n}\n\n#moss-pricing-app .model-card {\n  min-width: 0;\n  padding: 16px;\n\n  border: 1px solid var(--line);\n  border-radius: 13px;\n\n  background: var(--panel);\n\n  box-shadow:\n    inset 0 1px 0 rgba(255,255,255,.025);\n\n  transition:\n    transform .15s ease,\n    border-color .15s ease,\n    background .15s ease,\n    box-shadow .15s ease;\n}\n\n#moss-pricing-app .model-card:hover {\n  transform: translateY(-1px);\n\n  border-color: var(--line-strong);\n\n  background:\n    linear-gradient(180deg, var(--accent-soft), transparent 90px),\n    var(--panel);\n\n  box-shadow:\n    inset 0 1px 0 rgba(255,255,255,.03),\n    0 12px 26px rgba(0,0,0,.035);\n}\n\n#moss-pricing-app .model-card-top {\n  display: flex;\n  align-items: flex-start;\n  justify-content: space-between;\n  gap: 12px;\n}\n\n#moss-pricing-app .model-title-area {\n  min-width: 0;\n  flex: 1 1 auto;\n}\n\n#moss-pricing-app .model-name {\n  display: block;\n\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n\n  color: var(--text);\n\n  font-family:\n    ui-monospace,\n    SFMono-Regular,\n    Menlo,\n    Monaco,\n    Consolas,\n    monospace;\n\n  font-size: 14px;\n  line-height: 1.35;\n  font-weight: 760;\n}\n\n#moss-pricing-app .model-price-row {\n  margin-top: 7px;\n\n  display: flex;\n  flex-wrap: wrap;\n\n  gap: 6px 13px;\n\n  color: var(--muted);\n\n  font-size: 12px;\n  line-height: 1.4;\n}\n\n#moss-pricing-app .model-price-row b {\n  color: var(--text);\n\n  font-family:\n    ui-monospace,\n    SFMono-Regular,\n    Menlo,\n    Monaco,\n    Consolas,\n    monospace;\n\n  font-size: 12px;\n  font-weight: 700;\n}\n\n#moss-pricing-app .model-actions {\n  flex: 0 0 auto;\n\n  display: flex;\n  align-items: center;\n  gap: 6px;\n}\n\n#moss-pricing-app .detail-btn,\n#moss-pricing-app .copy-btn {\n  height: 34px;\n\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n\n  border: 1px solid var(--line);\n  border-radius: 9px;\n\n  background: var(--panel-strong);\n  color: var(--muted);\n\n  text-decoration: none;\n\n  cursor: pointer;\n\n  transition:\n    border-color .14s ease,\n    background .14s ease,\n    color .14s ease;\n}\n\n#moss-pricing-app .detail-btn {\n  padding: 0 11px;\n  gap: 5px;\n\n  font-size: 12px;\n  font-weight: 650;\n}\n\n#moss-pricing-app .copy-btn {\n  width: 34px;\n  padding: 0;\n}\n\n#moss-pricing-app .detail-btn:hover,\n#moss-pricing-app .copy-btn:hover {\n  border-color: var(--line-strong);\n  background: var(--accent-soft);\n  color: var(--text);\n}\n\n#moss-pricing-app .detail-btn svg {\n  width: 14px;\n  height: 14px;\n}\n\n#moss-pricing-app .copy-btn svg {\n  width: 14px;\n  height: 14px;\n}\n\n#moss-pricing-app .model-meta {\n  margin-top: 14px;\n  padding-top: 11px;\n\n  display: flex;\n  flex-wrap: wrap;\n  gap: 6px;\n\n  border-top: 1px solid var(--line);\n}\n\n#moss-pricing-app .meta-tag {\n  min-height: 23px;\n  padding: 0 8px;\n\n  display: inline-flex;\n  align-items: center;\n\n  border: 1px solid var(--line);\n  border-radius: 999px;\n\n  background: var(--panel-strong);\n  color: var(--muted);\n\n  font-size: 10px;\n  font-weight: 580;\n}\n\n#moss-pricing-app .meta-tag--provider {\n  color: var(--text);\n}\n\n/* ---------- states ---------- */\n\n#moss-pricing-app .state-box {\n  min-height: 180px;\n\n  display: grid;\n  place-items: center;\n\n  border: 1px dashed var(--line-strong);\n  border-radius: 13px;\n\n  color: var(--muted);\n\n  font-size: 14px;\n  text-align: center;\n}\n\n#moss-pricing-app .toast {\n  position: fixed;\n  left: 50%;\n  bottom: 36px;\n  z-index: 100;\n\n  transform: translateX(-50%);\n\n  min-height: 36px;\n  padding: 0 13px;\n\n  display: flex;\n  align-items: center;\n\n  border: 1px solid var(--line-strong);\n  border-radius: 9px;\n\n  background: var(--panel-strong);\n  color: var(--text);\n\n  box-shadow: 0 12px 30px rgba(0,0,0,.14);\n\n  font-size: 12px;\n\n  pointer-events: none;\n}\n\n/* ---------- mobile filter ---------- */\n\n#moss-pricing-app .mobile-filter-toggle {\n  display: none;\n}\n\n@media (max-width: 1180px) {\n  #moss-pricing-app .market-shell {\n    width: calc(100vw - 28px);\n  }\n\n  #moss-pricing-app .market-layout {\n    grid-template-columns: 250px minmax(0,1fr);\n  }\n\n  #moss-pricing-app .model-grid {\n    grid-template-columns: repeat(2,minmax(0,1fr));\n  }\n}\n\n@media (max-width: 820px) {\n  #moss-pricing-app {\n    inset: 56px 0 0 0;\n  }\n\n  #moss-pricing-app .market-shell {\n    width: calc(100vw - 20px);\n    padding-top: 18px;\n  }\n\n  #moss-pricing-app .market-topbar {\n    align-items: stretch;\n    flex-direction: column;\n    gap: 12px;\n  }\n\n  #moss-pricing-app .search-wrap {\n    flex: none;\n    width: 100%;\n    min-width: 0;\n  }\n\n  #moss-pricing-app .market-layout {\n    display: block;\n  }\n\n  #moss-pricing-app .mobile-filter-toggle {\n    display: flex;\n    align-items: center;\n    justify-content: space-between;\n\n    width: 100%;\n    min-height: 44px;\n\n    margin-bottom: 10px;\n    padding: 0 13px;\n\n    border: 1px solid var(--line);\n    border-radius: 11px;\n\n    background: var(--panel);\n    color: var(--text);\n\n    font-size: 14px;\n    font-weight: 680;\n\n    cursor: pointer;\n  }\n\n  #moss-pricing-app .filter-panel {\n    position: static;\n    display: none;\n    margin-bottom: 12px;\n  }\n\n  #moss-pricing-app .filter-panel[data-open=\"true\"] {\n    display: block;\n  }\n\n  #moss-pricing-app .model-grid {\n    grid-template-columns: 1fr;\n    gap: 8px;\n  }\n\n  #moss-pricing-app .model-card {\n    padding: 14px;\n  }\n}\n\n@media (max-width: 420px) {\n  #moss-pricing-app .market-title {\n    font-size: 21px;\n  }\n\n  #moss-pricing-app .market-subtitle {\n    font-size: 12px;\n  }\n\n  #moss-pricing-app .model-name {\n    font-size: 13.5px;\n  }\n\n  #moss-pricing-app .model-price-row {\n    font-size: 11.5px;\n  }\n\n  #moss-pricing-app .detail-btn {\n    padding: 0 9px;\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  #moss-pricing-app *,\n  #moss-pricing-app {\n    transition: none !important;\n    animation: none !important;\n  }\n}\n";

const CLIENT_JS = "\n(() => {\n  \"use strict\";\n\n  const path = location.pathname.replace(/\\/+$/, \"\") || \"/\";\n\n  if (path !== \"/pricing\") return;\n\n  const ROOT_ID = \"moss-pricing-app\";\n\n  if (document.getElementById(ROOT_ID)) return;\n\n  const esc = (value) =>\n    String(value ?? \"\")\n      .replaceAll(\"&\", \"&amp;\")\n      .replaceAll(\"<\", \"&lt;\")\n      .replaceAll(\">\", \"&gt;\")\n      .replaceAll('\"', \"&quot;\")\n      .replaceAll(\"'\", \"&#039;\");\n\n  const normalize = (json) => {\n    if (Array.isArray(json?.data)) {\n      return {\n        models: json.data,\n        vendors: Array.isArray(json.vendors) ? json.vendors : [],\n        usableGroup: json.usable_group || {},\n        groupRatio: json.group_ratio || {},\n      };\n    }\n\n    if (json?.data && typeof json.data === \"object\") {\n      const body = json.data;\n\n      return {\n        models: Array.isArray(body.data)\n          ? body.data\n          : Array.isArray(body.models)\n            ? body.models\n            : [],\n        vendors: Array.isArray(body.vendors)\n          ? body.vendors\n          : Array.isArray(json.vendors)\n            ? json.vendors\n            : [],\n        usableGroup: body.usable_group || json.usable_group || {},\n        groupRatio: body.group_ratio || json.group_ratio || {},\n      };\n    }\n\n    return {\n      models: [],\n      vendors: [],\n      usableGroup: {},\n      groupRatio: {},\n    };\n  };\n\n  const inferProvider = (name) => {\n    const n = String(name || \"\").toLowerCase();\n\n    const rules = [\n      [/(gpt|openai|chatgpt|^o1|^o3|^o4)/, \"OpenAI\"],\n      [/(claude|anthropic)/, \"Anthropic\"],\n      [/(gemini|google)/, \"Google\"],\n      [/(deepseek)/, \"DeepSeek\"],\n      [/(qwen|qwq|tongyi)/, \"阿里巴巴\"],\n      [/(doubao|seed|bytedance)/, \"字节跳动\"],\n      [/(glm|zhipu|chatglm)/, \"智谱\"],\n      [/(kimi|moonshot)/, \"零一万物\"],\n      [/(minimax)/, \"MiniMax\"],\n      [/(grok|xai)/, \"xAI\"],\n      [/(llama|meta)/, \"Meta\"],\n      [/(mistral|mixtral)/, \"Mistral\"],\n    ];\n\n    for (const [pattern, provider] of rules) {\n      if (pattern.test(n)) return provider;\n    }\n\n    return \"其他\";\n  };\n\n  const pickNumber = (obj, keys) => {\n    for (const key of keys) {\n      const value = Number(obj?.[key]);\n      if (Number.isFinite(value)) return value;\n    }\n\n    return null;\n  };\n\n  const fmt = (num) => {\n    if (!Number.isFinite(num)) return \"—\";\n    if (Math.abs(num) >= 100) return num.toFixed(0);\n    if (Math.abs(num) >= 10) return num.toFixed(2).replace(/\\.?0+$/, \"\");\n    if (Math.abs(num) >= 1) return num.toFixed(3).replace(/\\.?0+$/, \"\");\n\n    return num\n      .toFixed(6)\n      .replace(/0+$/, \"\")\n      .replace(/\\.$/, \"\");\n  };\n\n  const getGroups = (model) => {\n    const raw =\n      model?.enable_group ??\n      model?.enable_groups ??\n      model?.groups ??\n      [];\n\n    if (Array.isArray(raw)) return raw.map(String);\n\n    if (typeof raw === \"string\") {\n      return raw\n        .split(\",\")\n        .map((item) => item.trim())\n        .filter(Boolean);\n    }\n\n    return [];\n  };\n\n  const getTags = (model) => {\n    const raw = model?.tags;\n\n    if (Array.isArray(raw)) return raw.map(String);\n\n    if (typeof raw === \"string\") {\n      return raw\n        .split(\",\")\n        .map((item) => item.trim())\n        .filter(Boolean);\n    }\n\n    return [];\n  };\n\n  const root = document.createElement(\"main\");\n  root.id = ROOT_ID;\n\n  root.innerHTML = `\n    <div class=\"market-shell\">\n      <header class=\"market-topbar\">\n        <div class=\"market-title-wrap\">\n          <h1 class=\"market-title\">模型广场</h1>\n          <p class=\"market-subtitle\">\n            全部 <span class=\"market-count\">—</span> 个模型在当前页面连续展示，不分页。\n          </p>\n        </div>\n\n        <label class=\"search-wrap\">\n          <svg\n            class=\"search-icon\"\n            viewBox=\"0 0 24 24\"\n            fill=\"none\"\n            stroke=\"currentColor\"\n            stroke-width=\"1.8\"\n            aria-hidden=\"true\"\n          >\n            <circle cx=\"11\" cy=\"11\" r=\"7\"></circle>\n            <path d=\"m20 20-3.4-3.4\"></path>\n          </svg>\n\n          <input\n            class=\"search-input\"\n            type=\"search\"\n            autocomplete=\"off\"\n            spellcheck=\"false\"\n            placeholder=\"搜索模型名称、供应商或标签…\"\n            aria-label=\"搜索模型\"\n          >\n        </label>\n      </header>\n\n      <button\n        type=\"button\"\n        class=\"mobile-filter-toggle\"\n        aria-expanded=\"false\"\n      >\n        <span>筛选</span>\n        <span>展开</span>\n      </button>\n\n      <div class=\"market-layout\">\n        <aside class=\"filter-panel\" data-open=\"false\">\n          <div class=\"filter-head\">\n            <div>\n              <h2>筛选</h2>\n              <p>按供应商、分组和标签细化模型。</p>\n            </div>\n\n            <button type=\"button\" class=\"reset-btn\">\n              重置\n            </button>\n          </div>\n\n          <section class=\"filter-section\">\n            <h3 class=\"filter-section-title\">分组</h3>\n            <div class=\"filter-chips group-chips\"></div>\n          </section>\n\n          <section class=\"filter-section\">\n            <h3 class=\"filter-section-title\">所有供应商</h3>\n            <div class=\"filter-chips vendor-chips\"></div>\n          </section>\n\n          <section class=\"filter-section tag-filter-section\">\n            <h3 class=\"filter-section-title\">标签</h3>\n            <div class=\"filter-chips tag-chips\"></div>\n          </section>\n        </aside>\n\n        <section class=\"result-panel\">\n          <div class=\"result-head\">\n            <div class=\"result-info\">\n              显示 <strong class=\"visible-count\">—</strong> 个模型\n            </div>\n\n            <select class=\"sort-select\" aria-label=\"模型排序\">\n              <option value=\"name-asc\">名称 A → Z</option>\n              <option value=\"name-desc\">名称 Z → A</option>\n              <option value=\"provider\">按供应商</option>\n            </select>\n          </div>\n\n          <div class=\"model-grid\">\n            <div class=\"state-box\">\n              正在加载全部模型…\n            </div>\n          </div>\n        </section>\n      </div>\n    </div>\n  `;\n\n  document.body.appendChild(root);\n\n  const grid = root.querySelector(\".model-grid\");\n  const totalCount = root.querySelector(\".market-count\");\n  const visibleCount = root.querySelector(\".visible-count\");\n  const searchInput = root.querySelector(\".search-input\");\n  const groupChips = root.querySelector(\".group-chips\");\n  const vendorChips = root.querySelector(\".vendor-chips\");\n  const tagChips = root.querySelector(\".tag-chips\");\n  const tagSection = root.querySelector(\".tag-filter-section\");\n  const resetBtn = root.querySelector(\".reset-btn\");\n  const sortSelect = root.querySelector(\".sort-select\");\n  const filterPanel = root.querySelector(\".filter-panel\");\n  const mobileFilterToggle = root.querySelector(\".mobile-filter-toggle\");\n\n  let allModels = [];\n  let vendors = [];\n  let activeGroup = \"全部\";\n  let activeVendor = \"全部\";\n  let activeTag = \"全部\";\n  let searchQuery = \"\";\n\n  const toast = (message) => {\n    root.querySelector(\".toast\")?.remove();\n\n    const el = document.createElement(\"div\");\n    el.className = \"toast\";\n    el.textContent = message;\n\n    root.appendChild(el);\n\n    window.setTimeout(() => el.remove(), 1300);\n  };\n\n  const copyText = async (text) => {\n    try {\n      await navigator.clipboard.writeText(text);\n    } catch {\n      const area = document.createElement(\"textarea\");\n      area.value = text;\n      area.style.position = \"fixed\";\n      area.style.opacity = \"0\";\n\n      document.body.appendChild(area);\n      area.select();\n\n      try {\n        document.execCommand(\"copy\");\n      } catch {}\n\n      area.remove();\n    }\n\n    toast(\"已复制：\" + text);\n  };\n\n  const vendorMap = () => {\n    const map = new Map();\n\n    for (const vendor of vendors) {\n      map.set(String(vendor?.id), vendor);\n    }\n\n    return map;\n  };\n\n  const providerOf = (model, map) => {\n    const vendor = map.get(String(model?.vendor_id));\n\n    return (\n      vendor?.name ||\n      model?.vendor_name ||\n      inferProvider(model?.model_name)\n    );\n  };\n\n  const priceHTML = (model) => {\n    const input = pickNumber(model, [\n      \"input_price\",\n      \"prompt_price\",\n      \"input_price_per_m\",\n      \"input_price_per_1m\",\n      \"prompt_price_per_m\",\n    ]);\n\n    const output = pickNumber(model, [\n      \"output_price\",\n      \"completion_price\",\n      \"output_price_per_m\",\n      \"output_price_per_1m\",\n      \"completion_price_per_m\",\n    ]);\n\n    const fixed = pickNumber(model, [\"model_price\"]);\n    const modelRatio = pickNumber(model, [\"model_ratio\"]);\n    const completionRatio = pickNumber(model, [\"completion_ratio\"]);\n\n    if (input !== null || output !== null) {\n      return [\n        input !== null\n          ? `<span>输入 <b>$${fmt(input)}</b></span>`\n          : \"\",\n        output !== null\n          ? `<span>输出 <b>$${fmt(output)}</b></span>`\n          : \"\",\n      ].join(\"\");\n    }\n\n    if (Number(model?.quota_type) === 1 && fixed !== null) {\n      return `<span>固定 <b>$${fmt(fixed)}</b></span>`;\n    }\n\n    if (modelRatio !== null) {\n      const outRatio =\n        completionRatio !== null\n          ? modelRatio * completionRatio\n          : null;\n\n      return [\n        `<span>输入 <b>x${fmt(modelRatio)}</b></span>`,\n        outRatio !== null\n          ? `<span>输出 <b>x${fmt(outRatio)}</b></span>`\n          : \"\",\n      ].join(\"\");\n    }\n\n    return `<span>价格 <b>—</b></span>`;\n  };\n\n  const cardHTML = (item) => {\n    const model = item.model;\n    const name = String(model?.model_name || \"\");\n    const groups = item.groups;\n    const tags = item.tags;\n\n    const detailHref =\n      \"/pricing/\" +\n      encodeURIComponent(name);\n\n    return `\n      <article\n        class=\"model-card\"\n        data-name=\"${esc(name.toLowerCase())}\"\n        data-provider=\"${esc(item.provider.toLowerCase())}\"\n      >\n        <div class=\"model-card-top\">\n          <div class=\"model-title-area\">\n            <span\n              class=\"model-name\"\n              title=\"${esc(name)}\"\n            >${esc(name)}</span>\n\n            <div class=\"model-price-row\">\n              ${priceHTML(model)}\n            </div>\n          </div>\n\n          <div class=\"model-actions\">\n            <a\n              class=\"detail-btn\"\n              href=\"${esc(detailHref)}\"\n            >\n              <span>详情</span>\n              <svg\n                viewBox=\"0 0 24 24\"\n                fill=\"none\"\n                stroke=\"currentColor\"\n                stroke-width=\"1.8\"\n                aria-hidden=\"true\"\n              >\n                <path d=\"m9 18 6-6-6-6\"></path>\n              </svg>\n            </a>\n\n            <button\n              type=\"button\"\n              class=\"copy-btn\"\n              data-copy-model=\"${esc(name)}\"\n              title=\"复制\"\n              aria-label=\"复制模型名称\"\n            >\n              <svg\n                viewBox=\"0 0 24 24\"\n                fill=\"none\"\n                stroke=\"currentColor\"\n                stroke-width=\"1.8\"\n                aria-hidden=\"true\"\n              >\n                <rect x=\"9\" y=\"9\" width=\"10\" height=\"10\" rx=\"2\"></rect>\n                <path d=\"M15 9V7a2 2 0 0 0-2-2H7a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2h2\"></path>\n              </svg>\n            </button>\n          </div>\n        </div>\n\n        <div class=\"model-meta\">\n          <span class=\"meta-tag meta-tag--provider\">\n            ${esc(item.provider)}\n          </span>\n\n          ${groups.slice(0, 2).map(\n            (group) =>\n              `<span class=\"meta-tag\">${esc(group)}</span>`\n          ).join(\"\")}\n\n          ${tags.slice(0, 2).map(\n            (tag) =>\n              `<span class=\"meta-tag\">${esc(tag)}</span>`\n          ).join(\"\")}\n        </div>\n      </article>\n    `;\n  };\n\n  const chipHTML = (label, count, type, active) => `\n    <button\n      type=\"button\"\n      class=\"filter-chip\"\n      data-filter-type=\"${esc(type)}\"\n      data-filter-value=\"${esc(label)}\"\n      aria-pressed=\"${active ? \"true\" : \"false\"}\"\n    >\n      <span>${esc(label)}</span>\n      <span class=\"filter-chip-count\">${count}</span>\n    </button>\n  `;\n\n  const buildFilterOptions = () => {\n    const groupCounts = new Map();\n    const vendorCounts = new Map();\n    const tagCounts = new Map();\n\n    for (const item of allModels) {\n      vendorCounts.set(\n        item.provider,\n        (vendorCounts.get(item.provider) || 0) + 1\n      );\n\n      for (const group of item.groups) {\n        groupCounts.set(\n          group,\n          (groupCounts.get(group) || 0) + 1\n        );\n      }\n\n      for (const tag of item.tags) {\n        tagCounts.set(\n          tag,\n          (tagCounts.get(tag) || 0) + 1\n        );\n      }\n    }\n\n    const total = allModels.length;\n\n    groupChips.innerHTML =\n      chipHTML(\"全部\", total, \"group\", activeGroup === \"全部\") +\n      [...groupCounts.entries()]\n        .sort((a, b) => a[0].localeCompare(b[0], \"zh-CN\"))\n        .map(([label, count]) =>\n          chipHTML(label, count, \"group\", activeGroup === label)\n        )\n        .join(\"\");\n\n    vendorChips.innerHTML =\n      chipHTML(\"全部\", total, \"vendor\", activeVendor === \"全部\") +\n      [...vendorCounts.entries()]\n        .sort((a, b) => b[1] - a[1] || a[0].localeCompare(b[0], \"zh-CN\"))\n        .map(([label, count]) =>\n          chipHTML(label, count, \"vendor\", activeVendor === label)\n        )\n        .join(\"\");\n\n    const usefulTags = [...tagCounts.entries()]\n      .sort((a, b) => b[1] - a[1] || a[0].localeCompare(b[0], \"zh-CN\"))\n      .slice(0, 18);\n\n    if (usefulTags.length) {\n      tagSection.style.display = \"\";\n\n      tagChips.innerHTML =\n        chipHTML(\"全部\", total, \"tag\", activeTag === \"全部\") +\n        usefulTags\n          .map(([label, count]) =>\n            chipHTML(label, count, \"tag\", activeTag === label)\n          )\n          .join(\"\");\n    } else {\n      tagSection.style.display = \"none\";\n    }\n  };\n\n  const filteredModels = () => {\n    let result = allModels.filter((item) => {\n      const searchHaystack = [\n        item.name,\n        item.provider,\n        ...item.groups,\n        ...item.tags,\n      ]\n        .join(\" \")\n        .toLowerCase();\n\n      if (\n        searchQuery &&\n        !searchHaystack.includes(searchQuery)\n      ) {\n        return false;\n      }\n\n      if (\n        activeGroup !== \"全部\" &&\n        !item.groups.includes(activeGroup)\n      ) {\n        return false;\n      }\n\n      if (\n        activeVendor !== \"全部\" &&\n        item.provider !== activeVendor\n      ) {\n        return false;\n      }\n\n      if (\n        activeTag !== \"全部\" &&\n        !item.tags.includes(activeTag)\n      ) {\n        return false;\n      }\n\n      return true;\n    });\n\n    if (sortSelect.value === \"name-desc\") {\n      result.sort((a, b) =>\n        b.name.localeCompare(a.name, undefined, {\n          numeric: true,\n          sensitivity: \"base\",\n        })\n      );\n    } else if (sortSelect.value === \"provider\") {\n      result.sort(\n        (a, b) =>\n          a.provider.localeCompare(b.provider, \"zh-CN\") ||\n          a.name.localeCompare(b.name, undefined, {\n            numeric: true,\n            sensitivity: \"base\",\n          })\n      );\n    } else {\n      result.sort((a, b) =>\n        a.name.localeCompare(b.name, undefined, {\n          numeric: true,\n          sensitivity: \"base\",\n        })\n      );\n    }\n\n    return result;\n  };\n\n  const render = () => {\n    const result = filteredModels();\n\n    visibleCount.textContent = String(result.length);\n\n    if (!result.length) {\n      grid.innerHTML = `\n        <div class=\"state-box\">\n          没有匹配的模型。\n        </div>\n      `;\n\n      return;\n    }\n\n    grid.innerHTML =\n      result\n        .map(cardHTML)\n        .join(\"\");\n\n    grid\n      .querySelectorAll(\"[data-copy-model]\")\n      .forEach((button) => {\n        button.addEventListener(\"click\", () =>\n          copyText(\n            button.getAttribute(\"data-copy-model\") || \"\"\n          )\n        );\n      });\n  };\n\n  root.addEventListener(\"click\", (event) => {\n    const chip =\n      event.target.closest(\".filter-chip\");\n\n    if (!chip) return;\n\n    const type =\n      chip.getAttribute(\"data-filter-type\");\n\n    const value =\n      chip.getAttribute(\"data-filter-value\") || \"全部\";\n\n    if (type === \"group\") activeGroup = value;\n    if (type === \"vendor\") activeVendor = value;\n    if (type === \"tag\") activeTag = value;\n\n    buildFilterOptions();\n    render();\n  });\n\n  searchInput.addEventListener(\"input\", () => {\n    searchQuery =\n      searchInput.value\n        .trim()\n        .toLowerCase();\n\n    render();\n  });\n\n  sortSelect.addEventListener(\"change\", render);\n\n  resetBtn.addEventListener(\"click\", () => {\n    activeGroup = \"全部\";\n    activeVendor = \"全部\";\n    activeTag = \"全部\";\n    searchQuery = \"\";\n    searchInput.value = \"\";\n    sortSelect.value = \"name-asc\";\n\n    buildFilterOptions();\n    render();\n  });\n\n  mobileFilterToggle.addEventListener(\"click\", () => {\n    const isOpen =\n      filterPanel.getAttribute(\"data-open\") === \"true\";\n\n    filterPanel.setAttribute(\n      \"data-open\",\n      isOpen ? \"false\" : \"true\"\n    );\n\n    mobileFilterToggle.setAttribute(\n      \"aria-expanded\",\n      isOpen ? \"false\" : \"true\"\n    );\n\n    mobileFilterToggle.lastElementChild.textContent =\n      isOpen ? \"展开\" : \"收起\";\n  });\n\n  fetch(\"/api/pricing\", {\n    method: \"GET\",\n    credentials: \"include\",\n    headers: {\n      Accept: \"application/json\",\n    },\n    cache: \"no-store\",\n  })\n    .then((response) => {\n      if (!response.ok) {\n        throw new Error(\"HTTP \" + response.status);\n      }\n\n      return response.json();\n    })\n    .then((json) => {\n      const normalized = normalize(json);\n      vendors = normalized.vendors;\n\n      const map = vendorMap();\n\n      allModels = normalized.models.map((model) => {\n        const name =\n          String(model?.model_name || \"\");\n\n        return {\n          model,\n          name,\n          provider: providerOf(model, map),\n          groups: getGroups(model),\n          tags: getTags(model),\n        };\n      });\n\n      totalCount.textContent =\n        String(allModels.length);\n\n      buildFilterOptions();\n      render();\n    })\n    .catch((error) => {\n      console.error(\n        \"[MOSS Pricing V2.2]\",\n        error\n      );\n\n      totalCount.textContent = \"0\";\n      visibleCount.textContent = \"0\";\n\n      grid.innerHTML = `\n        <div class=\"state-box\">\n          模型数据加载失败，请刷新后重试。\n        </div>\n      `;\n    });\n})();\n";

class HeadInjector {
  element(element) {
    element.append(
      '<style id="moss-pricing-v22-style">' + GLOBAL_CSS + '</style>',
      { html: true }
    );
  }
}

class BodyInjector {
  element(element) {
    const safeClientJs =
      CLIENT_JS.replaceAll('</script', '<\\/script');

    element.append(
      '<script id="moss-pricing-v22-script">' +
        safeClientJs +
      '</script>',
      { html: true }
    );
  }
}

function withDebugHeader(response, value) {
  const headers =
    new Headers(response.headers);

  headers.set(
    "X-MOSS-Pricing-Skin",
    value
  );

  return new Response(
    response.body,
    {
      status: response.status,
      statusText: response.statusText,
      headers,
    }
  );
}

export default {
  async fetch(request) {
    const url =
      new URL(request.url);

    if (
      url.pathname !== "/pricing" &&
      !url.pathname.startsWith("/pricing/")
    ) {
      return fetch(request);
    }

    const upstream =
      await fetch(request);

    const contentType =
      upstream.headers.get("content-type") || "";

    if (
      !contentType
        .toLowerCase()
        .includes("text/html")
    ) {
      return withDebugHeader(
        upstream,
        "bypass-non-html"
      );
    }

    const transformed =
      new HTMLRewriter()
        .on("head", new HeadInjector())
        .on("body", new BodyInjector())
        .transform(upstream);

    return withDebugHeader(
      transformed,
      "active-v2.2-filter-detail-large-font"
    );
  },
};
